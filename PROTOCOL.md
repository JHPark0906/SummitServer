# Summit 프로토콜 v6

클라이언트와 서버는 이 문서의 schema 6 계약을 사용한다. 전원 프로필 명단과 AOI 가시 객체는 별도로 관리하며, 초기 명단은 ACK 페이지로, 위치는 주기적 상태 배치로 전달한다. `schemaVersion`이 6이 아닌 Join은 `schema_mismatch`로 거절한다. 전체 채팅과 서버 공지를 지원하며 `CatLaunch` handler는 없다.
가입·명단·프로필·채팅·가시 객체 생성/삭제는 TCP 프레임(4바이트 little-endian 본문 길이 + UTF-8 JSON)을 사용한다. 이동은 Join에서 선택한 UDP 경로를 지원하며, 선택하지 않은 클라이언트는 TCP 이동을 사용한다.
세션 ID와 서버 상태 revision `r`은 정밀도 손실을 피하기 위해 10진수 문자열로 전송한다.

입력·이동·점프·충돌 판정은 클라이언트가 수행한다. 서버는 클라이언트의 최신 위치·속도를 검증하고 보관해 주변 수신자에게 복제하며, 이동 물리를 시뮬레이션하거나 서버 권위 이동 검증을 수행하지 않는다. 자기 캐릭터는 서버 응답을 기다리지 않고 움직이고, 복제 지연은 다른 참가자의 움직임이 보이는 데 영향을 준다.

## 서버 접속 설정

`SummitServer`의 기본 수신 주소는 `0.0.0.0:17890`이다. 모든 로컬 IPv4 인터페이스에서 TCP 연결과 같은 포트의 UDP datagram을 받는다. 클라이언트의 접속 주소는 클라이언트 설정에서 선택한다. 공유기의 공인 IP를 서버 소켓에 직접 바인딩하지 않는다.

실행 시 `--address <IPv4>`와 `--port <1-65535>`로 기본값을 바꿀 수 있다. 로컬 테스트만 받으려면 `SummitServer --address 127.0.0.1 --port 17890`으로 실행하고 클라이언트도 `127.0.0.1:17890`에 접속한다. 다른 PC에서 접속하려면 **TCP 17890과 UDP 17890 모두** 도달할 수 있어야 하며, 방화벽 허용과 NAT 환경의 포트포워딩은 운영 환경에 맞게 설정한다. `0.0.0.0`은 수신용 wildcard이며 클라이언트 접속 주소로 사용하지 않는다. `--port`를 바꾸면 두 전송이 함께 해당 번호를 쓴다. 서버는 방화벽이나 공유기 설정을 변경하지 않는다. UDP bind 실패도 시작 실패이며, 실행 중 자동으로 TCP 이동으로 전환하지 않는다.

`--max-sessions <1-65536>`을 지정하면 TCP 연결 상한과 가입 플레이어 상한을 함께 그 값으로 설정한다. 생략하면 64개 연결·16명 플레이어 기본값을 사용한다. 포트 번호는 65535까지이고 세션 수는 65536까지이므로 두 범위는 다르다. 변경은 이번 프로세스에만 적용하며 다음 실행의 기본값을 저장하지 않는다.

```powershell
.\SummitServer.exe --port 17890 --max-sessions 65536
# 같은 PC에서 부하 검증할 때는 별도 loopback 포트를 사용한다.
.\SummitServer.exe --address 127.0.0.1 --port 17891 --max-sessions 65536 --metrics-interval-ms 1000
```

`--metrics-interval-ms`는 0이면 끄고, 양수이면 해당 간격 이상 지난 다음 서버 타이머 tick에 JSON 통계를 출력한다. 활성 세션·수신 및 송신 프레임 누계·서버 오류·수신/파싱/송신 대기량을 포함한다. 종료 시 `last_sample_before_stop`은 마지막 관측값이며 종료 후 상태를 뜻하지 않는다. 최대 세션 설정은 자원 사용과 처리량을 보장하지 않으며, 전체 플레이어 간 방송은 여전히 O(N²) 비용을 가진다.

간단한 더미 프로그램은 같은 빌드의 `SummitLoadTest.exe`다. [부하 검증 사용법](tests/load/README.md)에 실행용 cmd와 단계별 예제가 있다.

## 이벤트 로그

서버 콘솔에는 `[INFO]`와 JSON 한 줄 형태로 승인된 `player_joined`, `player_left`, `chat`, `announcement` 이벤트를 기록한다. 입장에는 닉네임·세션 ID·확정 캐릭터, 퇴장에는 현재 승인된 프로필과 종료 사유 코드, 채팅에는 서버가 확인한 발신자·내용·프로세스 시작 이후 시각을 담는다. 공지는 운영자가 승인받은 내용과 서버 시각을 기록하며, 거절된 입력과 매 프레임 이동은 이벤트 로그에 넣지 않는다.

닉네임과 채팅은 JSON escape를 거치며 UTF-8로 출력한다. 로깅 실패가 게임 처리 결과를 바꾸지는 않는다. 기본은 콘솔 출력이고 파일 보관이 필요하면 실행할 때 표준 출력과 오류를 함께 리다이렉트한다.

```powershell
.\SummitServer.exe --port 17890 --max-sessions 65536 > server.log 2>&1
```

## 입장과 프로필

```json
{"type":"Join","body":{"schemaVersion":6,"name":"용사","c":0,"movementTransport":"udp"}}
```

- `name`: UTF-8 1~48바이트. 빈 문자열, ASCII 공백만 있는 이름, ASCII 제어문자는 거절한다. 중복 검사는 대소문자를 구별하는 문자열 비교다.
- 요청 `c`: 정수 `0`(A), `1`(B), `2`(C), `3`(D), `4`(E) 중 하나다. `5`(tdw)는 직접 선택할 수 없으며 `character_invalid`로 거절한다.
- 서버는 `name`이 정확히 UTF-8 `상여자` 또는 `심심이심셔`이면 요청한 일반 캐릭터와 관계없이 최종 `c`를 `5`(tdw)로 정한다. 이름의 앞뒤 공백을 제거하거나 유니코드를 정규화하지 않는다. 따라서 `상여자 `는 별개의 일반 이름이다. 전용 이름에도 요청 `c`의 유효성 검사는 적용한다.
- 클라이언트는 일반 선택값 A~E를 별도로 보관하고, tdw가 적용된 상태의 재접속이나 프로필 변경에서도 요청에는 그 일반 선택값을 보낸다. 전용 이름에서 일반 이름으로 바꾸면 해당 요청의 A~E 선택이 적용된다.
- 승인: `JoinAccepted.body`에 `schemaVersion:6`, `id`, `name`, `c`, `capacity`, `players:[]`, `directoryThroughId`를 보낸다. `c`는 서버가 확정하여 저장한 `0..5`이며, `capacity`는 이번 실행의 가입 상한이다. `directoryThroughId`는 이 가입 시점 명단의 최대 세션 ID를 나타내는 10진수 문자열이다.
- `movementTransport`는 생략하거나 정확히 `"udp"`를 보낸다. 다른 값/타입은 `protocol_violation`이다. 서버에 이동 전송이 연결돼 있으면 UDP 선택 요청의 승인에 `udp:{port,token}`을 추가한다. `token`은 암호학적 난수로 생성한 16바이트를 나타내는 32자리 hex 문자열이다. 서버에 전송이 연결되지 않은 테스트/임베딩 환경은 이 필드를 생략하고 TCP를 유지한다. 실제 서버 프로그램은 UDP를 구성한다.
- 초기 전원 명단은 아래 `DirectoryPage`로 받는다. 실제 새 가입은 기존 접속자에게 `PlayerJoined {id,name,c}`로 알린다. 해당 이벤트는 가벼운 전원 프로필 명단만 갱신하며 화면에 원격 객체를 만드는 근거가 아니다.
- `JoinRejected.error.code`가 `name_duplicate`, `name_invalid`, `character_invalid`이면 같은 연결에서 Join을 재시도할 수 있다. 다른 입장 오류는 최종 거절 프레임 이후 연결을 종료한다.
- 최종 프레임의 송신이 막히면 ServerCore의 `gracefulCloseTimeout`(기본 5초) 이후 남은 송신 큐를 버리고 연결을 닫는다. 유휴 제한과 별개이며, 종료 중 수신한 바이트로 기한을 연장하지 않는다. 실제 종료 시점에는 주기 검사와 작업 대기 시간이 더해질 수 있다.

## 초기 전원 명단과 페이지 ACK

```json
{"type":"DirectoryPage","body":{"players":[{"id":"1","name":"용사","c":0}],"cursor":"1"}}
{"type":"DirectoryAck","body":{"cursor":"1"}}
{"type":"DirectoryReady","body":{}}
```

각 50ms 복제 tick에서 ACK를 기다리는 페이지가 없을 때 다음 ID순 페이지를 보낸다. 페이지는 최대 32명이며 실제 UTF-8 JSON envelope가 8KiB를 넘으면 항목 수를 줄인다. 자기 자신과 `directoryThroughId`를 넘는 ID는 초기 페이지에 넣지 않는다. 클라이언트는 페이지를 적용한 뒤 정확히 받은 `cursor` 문자열을 ACK한다. 한 페이지만 outstanding이므로 초기 전체 명단을 연결별 1MiB 송신 큐에 한 번에 넣지 않는다.

페이지는 송신 시점의 최신 승인 프로필을 읽는다. 페이지 사이에도 `PlayerJoined`, `ProfileChanged`, `PlayerLeft` 전역 이벤트를 즉시 적용한다. 이미 삭제된 ID는 이후 페이지에 나타나지 않고, 변경된 프로필은 최신 값으로 페이지에 들어가므로 별도의 거대한 snapshot 복사나 무제한 delta 이력을 보관하지 않는다. 클라이언트는 각 페이지를 ID 기준으로 upsert하며 페이지 도착 시 기존 명단을 지우지 않는다. 마지막 페이지 ACK 뒤 더 이상 초기 범위 항목이 없으면 `DirectoryReady`를 한 번 보낸다. 빈 초기 명단은 페이지 없이 완료할 수 있다.

ACK는 가입한 세션의 현재 outstanding cursor와 일치해야 한다. 숫자 타입, 앞의 0, 부호, 오버플로, 다른 cursor 및 중복 ACK는 `InvalidFormat`으로 종료하며 대기 기한을 연장하지 않는다. 가입 전 ACK는 `protocol_violation` 최종 거절 뒤 종료한다. 페이지 송신부터 30초 동안 정확한 ACK가 없으면 `Timeout`으로 종료한다. 페이지·완료 메시지는 아래 제어 예산을 사용한다. 예산 부족이나 `WouldBlock`은 cursor·완료 상태를 확정하지 않고 다음 tick에 재시도한다. 성공적으로 페이지를 넣은 뒤에만 ACK 기한을 시작한다. 그 밖의 송신 오류는 해당 가입자를 종료하고 전역 퇴장 처리를 수행한다.

## 세션 내 변경

```json
{"type":"SetProfile","body":{"name":"새용사","c":1}}
```

가입이 승인된 세션만 변경할 수 있다. 서버는 Join과 같은 규칙으로 닉네임과 요청 캐릭터를 검증하고, 전용 이름에 따른 최종 캐릭터를 함께 저장한다. 자신의 기존 이름은 중복으로 취급하지 않으며, 변경 후 이전 이름은 다시 사용할 수 있다. 이 가입 승인은 사용자 계정 인증을 뜻하지 않는다.

- 성공: 요청자를 **포함한 모든 가입자**에게 `ProfileChanged.body {id,name,c}`를 전송한다. 요청자도 이 메시지를 받은 뒤 로컬 프로필을 적용한다.
- 실패: 요청자에게만 `ProfileRejected.error.code`를 전송한다. 코드와 유효성 규칙은 위와 같다. 기존 프로필과 연결은 유지된다.
- 프로필 변경과 가입/퇴장 이벤트는 누락되면 복구할 후속 스냅샷이 없으므로, 전송 실패한 수신자는 연결을 종료한다. 한 수신자의 전송 예외 때문에 나머지 수신자에게 알림이 누락되지 않도록 처리한다.
- 가입/프로필 이벤트 처리 중 발생한 퇴장은 해당 이벤트 전송이 끝난 뒤 순서대로 처리한다. 메모리 부족으로 퇴장 메시지를 만들지 못하면 남은 수신자의 연결도 종료해, 사라진 플레이어가 계속 표시되는 상태를 남기지 않는다.
- 입장 전 `SetProfile`은 `protocol_violation`으로 연결을 종료한다.

## 이동과 퇴장

`PlayerState.body`는 `{x,y,vx,vy,f,s,c,q}`다. `x/y`는 유한한 값이고 절댓값이 1,000,000 이하여야 한다. `vx/vy/f`는 유한한 float 표현 범위의 숫자, `s`는 최대 32 UTF-8바이트 문자열, 요청 `c`는 정수 `0..5`다. 값 검증은 TCP와 UDP에서 공유한다. 잘못되거나 빠진 이동 필드를 TCP로 보내면 `InvalidFormat`으로 종료하고, UDP 입력은 버리고 기존 정상 상태와 TCP 연결을 유지한다.

`q`는 클라이언트 입력 순서다. UDP에서는 필수이며, 첫 유효 입력 이후 `q <= 마지막 승인 q`인 입력은 무시한다. TCP에서는 생략 시 0이며 q 순서 제한을 적용하지 않는다. 숫자 `uint64`와 부호·불필요한 선행 0 없는 10진수 문자열을 모두 받는다. 정수 JSON은 uint64 전체 범위를 보존하고, 소수/지수 표기는 정확히 표현 가능한 정수 범위만 받는다. 출력 `q`는 숫자이므로 정확한 uint64 비교가 필요한 소비자는 정수 파서를 써야 한다. `q`와 아래 서버 `r`, datagram 머리의 패킷 순번은 서로 다른 순서다.

서버는 세션별 최신 상태 하나와 수신 시각 `t`를 저장하고, 송신자의 ID 및 **서버에 저장된 승인 캐릭터**를 붙여 AOI 대상에게 복제한다. `q`도 최신 상태와 함께 그대로 반송하므로 부하 측정에서 송신 이력을 식별할 수 있다. 이전 tick 사이의 중간 상태는 최신 값으로 병합한다. 유효 이동 갱신에는 서버 전체에서 증가하는 `r`을 붙이고, 이미 위치가 있는 플레이어의 승인 프로필 변경도 새 `r`과 준비된 상태 캐시 무효화를 수행한다. 클라이언트가 보낸 `r`은 사용하지 않는다. 이동을 서버에서 시뮬레이션하거나 속도·순간이동의 게임 규칙을 검증하는 기능은 없으며, AOI 좌표 범위 검사는 공간 인덱스의 안전한 정수 변환을 위한 것이다. 자기 상태는 반송하지 않는다.

클라이언트는 원격 프로필을 가입 목록/프로필 이벤트로 관리하고, 이동 패킷으로 닉네임이나 캐릭터를 바꾸지 않는다. 연결 종료 시 `PlayerLeft.body {id}`를 전송하고 닉네임을 해제한다.

`f`는 그림의 반전 여부가 아니라 캐릭터가 바라보는 논리 방향이다(`-1`: 왼쪽, `1`: 오른쪽). tdw처럼 원본 그림이 반대 방향이면 클라이언트가 별도로 반전한다. tdw의 2배 시각 배율과 idle 왕복 재생도 클라이언트가 캐릭터/애니메이션 상태에서 결정하며, 물리 크기나 프로토콜 좌표를 바꾸지 않는다.

## AOI 가시 집합과 상태 복제

```json
{"type":"VisibilityEnter","body":{"players":[{"id":"1","name":"용사","c":0,"x":2.5,"y":4.0,"vx":0.0,"vy":0.0,"f":1.0,"s":"idle","t":12345,"q":7,"r":"101"}]}}
{"type":"VisibilityReady","body":{}}
{"type":"StateBatch","body":{"states":[{"id":"1","c":0,"x":3.0,"y":4.0,"vx":2.0,"vy":0.0,"f":1.0,"s":"walk","t":12395,"q":8,"r":"102"}]}}
{"type":"VisibilityExit","body":{"ids":["1"]}}
```

- 서버는 한 변 16인 XY 해시 격자에 최신 유효 위치를 등록한다. 셀 좌표는 `floor(position / 16)`이며 음수에서도 0 방향 절삭을 사용하지 않는다. 주변 셀 후보를 찾은 뒤 실제 사각형으로 다시 검사하므로 셀이 같다는 이유만으로 보이지 않는다.
- 관찰자 위치 기준 진입 사각형의 반폭/반높이는 32/20, 이미 보이는 대상의 이탈 사각형은 36/24다. 경계는 포함한다. 바깥 여유 구간에 처음 진입한 대상은 생성하지 않으며, 안쪽 진입 범위를 통과한 대상은 바깥 이탈 범위를 벗어날 때까지 유지한다. 관찰자 자신의 이동과 같은 셀 안의 이동도 매 tick에 반영한다.
- 유효한 첫 위치가 없는 플레이어는 공간에 등록하지 않는다. 가입 후 30초까지 첫 상태가 없으면 종료한다. 명단 완료 전에도 상태를 보낼 수 있으며, 관찰자 본인의 `DirectoryReady`와 위치가 모두 준비된 뒤 가시 이벤트를 보낸다. 다른 플레이어가 아직 명단을 받고 있더라도 유효 위치가 있으면 대상이 될 수 있다.
- `VisibilityEnter.players`는 승인 프로필과 현재 위치·속도·방향·애니메이션·`t/q/r` 전체를 포함한다. 이 이벤트에서만 원격 객체를 생성하고 초기 위치를 바로 적용한다. 초기 Enter 묶음 이후 `VisibilityReady`를 한 번 보내며 빈 집합도 정상적으로 완료한다.
- `VisibilityExit.ids`는 화면 객체·보간·말풍선만 제거한다. 전역 명단과 채팅 입퇴장 알림에는 영향을 주지 않는다. 다시 들어오면 최신 전체 상태로 새 Enter를 보낸다. 실제 종료의 `PlayerLeft`는 전역 명단과 가시 객체를 모두 제거하며, 이후 별도 AOI Exit를 중복 전송하지 않는다.
- `StateBatch.states`는 현재 가시 대상의 최신 상태 중 예산·우선순위로 선택한 일부만 담는다. UDP에서는 마지막 상태 유실을 복구하기 위해 같은 revision도 재전송 후보가 된다. 배치에서 생략된 ID는 유지하며, 상태 배치로 객체를 새로 만들지 않는다. 각 배치는 최대 32항목이다. TCP envelope는 8KiB, UDP StateBatch는 헤더를 포함한 1,200바이트 이하이며 실제 바이트 크기에 맞춰 더 작게 나눈다.
- Exit를 먼저 보내고 새 Enter를 보낸 뒤 상태를 갱신한다. Enter/Exit/Ready의 예산 부족·`WouldBlock`은 해당 batch를 확정하지 않고 재시도한다. 다음 tick에는 현재 가시 범위를 다시 계산하므로 오래된 생성/삭제 FIFO를 쌓지 않는다. 나머지 제어 오류는 수신자를 종료한다. 아직 Enter가 수락되지 않은 대상이나 이미 범위를 벗어나 Exit가 보류 중인 대상에게 StateBatch를 보내지 않는다.
- StateBatch 송신이 수락돼야 그 관계의 revision과 대기 우선순위를 전진시킨다. TCP `WouldBlock`과 모든 UDP 송신 실패는 최신 상태를 유지해 재시도하며, UDP 경로 실패로 TCP 세션을 끊지 않는다. TCP의 다른 송신 오류는 종료한다. 이미 TCP 큐에 들어간 프레임은 덮어쓰지 않는다. 동기 종료 콜백은 tick 끝에 처리하지만 TCP와 UDP 사이에는 도착 순서 보장이 없으므로 클라이언트의 ID/`r` 검사가 필요하다.
- 한 관찰자당 최대 4,096개 가시 관계, 서버 전체 최대 4,000,000개 관계를 유지한다. 가시 관계 한도 초과와 복구 불가능한 제어 오류는 명시적으로 종료한다. 단순한 tick 예산 부족과 incremental 제어 `WouldBlock`은 위 재시도 규칙을 따른다. 일부 가까운 플레이어를 선별해 숨기는 동작은 없다. 각 tick의 작업량은 관찰자의 조회 셀과 후보·실제 관계 수에 비례하며, 한 곳의 밀집은 O(N²) 비용을 그대로 가진다. 채널·출발점 분산 등 별도 게임 정책은 적용하지 않는다.

전원 프로필·실제 입퇴장·채팅·공지는 AOI 밖의 사람에게도 전달한다. 초기 명단 수신 중 아직 명단에 없는 작성자의 채팅도 서버가 승인한 메시지의 `id/name`으로 패널에 표시할 수 있으며, 말풍선은 실제 가시 객체가 있을 때만 만든다. 전체 명단을 유지하는 가입 비용과 전역 채팅·공지 비용은 공간 복제로 없어지지 않는다.

## 이동 UDP와 순서·유실 처리

TCP 승인에 `udp`가 있으면 클라이언트는 서버의 해당 UDP 포트로 `UdpHello {}`를 보내고 `UdpReady {}`를 받는다. 응답이 유실될 수 있어 Hello는 더 큰 패킷 순번으로 재시도한다. 협상한 세션의 TCP `PlayerState`는 무시하며, 정상적인 UDP 경로가 준비될 때까지 TCP로 같은 이동을 중복 전송하지 않는다. 광고가 없었던 세션은 TCP 이동만 사용한다.

각 datagram은 독립적이며 TCP의 길이 머리를 붙이지 않는다.

| 오프셋 | 바이트 | 내용 |
| ---: | ---: | --- |
| 0 | 4 | ASCII `SMU1` |
| 4 | 16 | JoinAccepted로 받은 opaque token 원본 바이트 |
| 20 | 8 | **big-endian** unsigned 64-bit 패킷 순번, 1부터 시작 |
| 28 | 1..1172 | UTF-8 JSON envelope 하나 |

전체 datagram은 최대 1,200바이트다. UDP/IP 헤더는 이 표에 포함하지 않는다. 분할/재조립, 압축, 암호화, 재전송 ACK는 없다. TCP 길이는 little-endian이지만 이 머리의 순번은 big-endian이다. 방향마다 패킷 순번이 독립적으로 증가하며 0은 양쪽에서 거절한다. 서버는 수신한 과거·중복 패킷 순번을 거절한다. 클라이언트는 서로 다른 캐릭터를 담은 배치가 역순으로 도착할 수 있으므로 패킷 전체를 순번으로 버리지 않고, 아래의 개체별 `r`로 적용 여부를 결정한다. 순번을 순환시키는 대신 새 세션/토큰으로 재접속한다.

서버는 미등록 토큰에 응답하지 않고, 토큰·패킷 순번을 확인한 뒤 JSON을 읽는다. 유효한 더 최신 패킷은 반환 endpoint를 갱신할 수 있어 NAT 포트 변경을 허용한다. 등록은 실제 승인 가입의 수명을 따르며 Join 롤백과 TCP 종료 때 해제된다. 토큰은 연결과 UDP 상대를 대응시키는 bearer 값이다. 현재 TCP/UDP에는 암호화나 MAC가 없으므로 토큰을 볼 수 있는 네트워크 관찰자까지 방어하는 인증 방식은 아니다.

`VisibilityEnter`는 TCP로 현재 `r`을 전달하고 해당 ID의 baseline을 만든다. 클라이언트는 가시 객체가 없는 ID의 UDP 상태를 버리고, 해당 ID의 `r <= 마지막 적용 r`이면 무시한다. Exit/PlayerLeft 뒤 남은 datagram도 객체를 재생성하지 않는다. 다시 Enter한 때는 최신 baseline을 세우므로 이전 가시 수명의 지연 패킷이 위치를 되돌리지 못한다. 프로필은 신뢰 TCP 명단의 승인 값을 사용한다.

서버가 마지막으로 상태 전송을 수락한 뒤 **500ms 이상** 지나면, 바뀌지 않은 최신 상태도 다시 후보가 된다. 이는 유실된 마지막 정지 상태를 이후 전송으로 복구하려는 best-effort 처리다. 수신 ACK가 없으며 예산·우선순위·실제 tick 지연·네트워크 유실 때문에 500ms 내 도착이나 고정 갱신률을 보장하지 않는다. 유실된 중간 궤적을 과거 패킷으로 재생하지 않는다.

UDP 수신은 같은 Host JobRunner의 10ms 주기 Poll에서 처리한다. 한 Poll은 최대 4,096개 또는 약 1MiB의 수신 데이터를 처리하고 멈춘다(마지막 datagram 크기만큼 바이트 경계를 넘을 수 있음). OS 수신/송신 버퍼 요청은 각각 4MiB/1MiB이며, 클라이언트마다 별도 수신 스레드나 무제한 과거 이동 큐를 만들지 않는다. 파싱·이동 갱신·AOI tick은 여전히 서버 CPU를 사용한다.

UDP 이동은 TCP Host의 수신 활동 시각을 갱신하지 않는다. 가입한 클라이언트는 움직임 여부와 관계없이 **5초마다 TCP `Heartbeat {}`**를 보낸다. 응답과 게임 이벤트는 없으며 TCP idle 제한은 30초다. 가입 전 Heartbeat는 `protocol_violation`이다.

## 틱 바이트 예산과 최신 상태 스케줄러

| CLI | C++ SchedulerOptions | 기본값 |
| --- | --- | ---: |
| `--state-bytes-per-tick` | `perClientStateBytesPerTick` | 2,048 |
| `--total-state-bytes-per-tick` | `globalStateBytesPerTick` | 1,048,576 (1MiB) |
| `--control-bytes-per-tick` | `perClientControlBytesPerTick` | 16,384 (16KiB) |
| `--total-control-bytes-per-tick` | `globalControlBytesPerTick` | 262,144 (256KiB) |

각 값은 1,024..16,777,216바이트이며 0으로 한도를 끌 수 없다. **실제 직렬화한 envelope + TCP 머리 4바이트 또는 UDP 응용 머리 28바이트**를 성공 수락했을 때 차감한다. IP·UDP/TCP 커널 헤더와 재전송 트래픽은 포함하지 않으므로 링크 전체 대역폭의 엄밀한 상한은 아니다. UDP Hello/Ready 왕복은 이동 StateBatch 예산과 별도로 수신 Poll 상한을 따른다.

제어 예산은 점진적인 DirectoryPage/DirectoryReady/VisibilityEnter/VisibilityExit/VisibilityReady에 적용한다. 전역 PlayerJoined/PlayerLeft/ProfileChanged/ChatMessage/ServerNotice와 Join 승인 등은 기존 Host의 bounded 신뢰 송신 큐를 사용한다. 제어와 상태는 별도 전체 예산과 수신자 round-robin 커서를 사용한다. 전체 예산의 남은 조각이 1,024바이트보다 작으면 순회를 멈추고 다음 tick에서 다음 수신자부터 시작하므로, 예산을 언제나 마지막 바이트까지 쓰지는 않는다.

이동 후보의 우선순위는 최초 미송신 변경을 관측한 뒤의 대기 시간에 근거리 보너스(최대 250ms 상당)와 위치·속도·방향·애니메이션 또는 승인 프로필 변화 보너스(500ms 상당)를 더한다. `q/t`만 바뀐 입력은 의미 변화 보너스를 받지 않는다. 대기는 성공 수락 전까지 유지되며 보너스가 유한해 멀리 있는 마지막 정지 상태도 계속 움직이는 근거리 대상에 영구히 밀리지 않게 한다. 이것은 고정 최대 지연 보장은 아니다.

선택한 상태는 최신 revision별 `PreparedJsonValue`로 escape/숫자 인코딩을 한 번 준비해 여러 수신자의 배치에 재사용한다. `PrepareArrayMessage`는 준비된 항목을 봉투로 조립하며 JSON을 다시 파싱하지 않는다. 실제 TCP NetworkSession은 `SendPrepared`로 재직렬화 없이 큐에 넣고, UDP 전송은 같은 준비된 envelope에 datagram 머리만 붙인다.

프로필·채팅·입퇴장 방송도 동일한 봉투를 한 번 준비해 수신자에게 재사용한다. 준비 실패 시 기존 개별 Send 경로를 사용해 수신자별 실패 처리를 유지한다. 가시성은 실제 x/y 변경이 있을 때 움직인 플레이어와 이전·새 위치 주변 관찰자를 다시 계산 대상으로 표시한다. 같은 셀 안의 경계 이동도 포함하며, q·속도·방향·애니메이션·프로필만 바뀌면 상태 복제는 계속하되 가시 집합을 다시 만들지 않는다. 부분 Enter/Exit 및 Ready 송신이 모두 성공하기 전에는 재시도 표시를 유지한다. 밀집한 모든 플레이어가 계속 좌표를 바꾸면 많은 관찰자가 계속 재계산 대상이 되므로 이 최적화가 그 상황의 고정 tick 시간을 보장하지는 않는다.

TCP 이동에는 `QueuedSendBytes() + 새 프레임 <= max(8192, 클라이언트 이동 예산 × 4)` 관측 검사도 적용해 오래된 상태가 신뢰 큐에 더 쌓이는 것을 줄인다. 이 값은 예약이 아니므로 실제 수락은 Host 큐가 다시 결정한다. UDP에는 TCP backlog 검사를 적용하지 않는다. 예산이나 역압으로 보류된 이동은 과거 패킷이 아니라 최신 상태 하나로 남는다.

Metrics의 `sentStateBytes/sentControlBytes`, `lastTickStateBytes/lastTickControlBytes`는 위 수락 바이트이며, budget-limited tick과 state/control backpressure 횟수도 출력한다. `lastTickDurationMicroseconds/maxTickDurationMicroseconds`는 실행 게이트 획득 대기와 마지막 deferred-close drain을 제외한 tick 본체 시간이다. 50ms는 예약 주기이며 실제 20Hz 처리·사용자 수용량을 보장하지 않는다. 전체 명단/채팅 방송과 밀집 AOI의 후보·가시 관계 비용은 별도로 남는다.

## 채팅

```json
{"type":"Chat","body":{"text":"안녕하세요!"}}
{"type":"ChatMessage","body":{"id":"1","name":"용사","text":"안녕하세요!","t":12345}}
```

- 가입한 세션만 전송할 수 있다. 가입 전 채팅은 최종 `JoinRejected.error.code: protocol_violation` 뒤 연결을 종료한다.
- `text`는 UTF-8 1~512바이트의 문자열이어야 한다. 빈 문자열, ASCII 공백만 있는 문자열, ASCII 제어문자(C0 `U+0000..U+001F`와 DEL `U+007F`; 줄바꿈과 탭 포함), 필드 누락·타입 오류·길이 초과는 `ChatRejected.error.code: chat_invalid`로 거절한다. 앞뒤 공백을 제거하거나 유니코드를 정규화하지 않으며 허용한 본문은 그대로 전송한다.
- 유효하지 않은 UTF-8 바이트나 잘못된 JSON은 ServerCore 파서에서 `InvalidFormat`으로 연결을 종료한다. 이 경우에는 채팅 핸들러에 도달하지 않으므로 `ChatRejected`를 보낼 수 없다. 유효한 JSON에 포함된 위 본문 오류는 연결을 유지한다.
- 서버는 자신의 세션 ID, **전송 승인 시점의 닉네임**과 단조 증가 시간 `t`를 붙여, 요청자 **포함** 모든 현재 가입자에게 `ChatMessage`를 보낸다. 클라이언트가 보낸 `id`, `name`, `t`는 사용하지 않는다. 과거 메시지의 이름은 나중에 프로필이 바뀌어도 바꾸지 않는다.
- 클라이언트는 자기 메시지도 서버가 보낸 `ChatMessage`를 받아 한 번만 기록한다. 성공 시 별도 승인 응답은 없으며, 전송 요청 성공만으로 전달 완료를 뜻하지 않는다.
- 서버는 세션마다 첫 채팅을 즉시 허용한다. 마지막 승인 채팅으로부터 500ms 미만인 유효 요청은 `ChatRejected.error.code: chat_rate_limited`로 거절한다. 잘못된 본문이나 제한에 걸린 요청으로 대기 시간을 연장하지 않으며, 프로필 변경으로 제한을 초기화하지 않는다. 다른 세션의 제한과는 독립적이다.
- `ChatRejected`는 요청의 `seq`가 있으면 그대로 돌려주며 연결과 프로필을 유지한다. 채팅 중계와 오류 응답의 송신이 실패한 수신자는 연결을 종료한다. 한 수신자의 실패가 다른 수신자의 채팅 중계를 막지 않는다. 중계 중 발생한 퇴장 이벤트는 해당 채팅 이후 전달한다.
- 서버는 재전송을 위한 대화 이력을 유지하지 않으며, 뒤늦게 접속한 플레이어에게 과거 대화를 재생하지 않는다. 승인된 채팅 내용은 위 이벤트 로그에 기록한다.

## 서버 공지와 실제 입장·퇴장 알림

`ServerNotice`는 서버에서 클라이언트로만 보내는 부가 메시지다. `kind`, `text`, `t`는 필수이며, `t`는 서버 프로세스 시작 이후 밀리초인 `uint64` 값이다. 입장·퇴장은 승인된 `id`(10진수 문자열)와 `name`도 포함한다. 공지에는 플레이어를 나타내는 `id`와 `name`을 넣지 않는다.

실행 중인 서버 콘솔에서 `/announce 공지 내용`을 입력한다. `/help`로 사용법을 볼 수 있다. Windows 콘솔 입력은 한글을 지원하며, 리디렉션 입력은 UTF-8(BOM 허용), LF/CRLF 및 마지막 EOF 줄을 받는다. EOF만으로 서버가 종료되지는 않으며 기존 Ctrl+C 종료를 사용한다. 입력 줄은 2048단위, 대기 중인 공지·목록 조회 명령의 합은 64개로 제한하므로 바쁨 안내를 받으면 나중에 재시도한다.

```json
{"type":"ServerNotice","body":{"kind":"announcement","text":"잠시 후 서버를 종료합니다.","t":12345}}
{"type":"ServerNotice","body":{"kind":"join","text":"용사님이 입장했습니다.","t":12346,"id":"1","name":"용사"}}
{"type":"ServerNotice","body":{"kind":"leave","text":"용사님이 퇴장했습니다.","t":23456,"id":"1","name":"용사"}}
```

- 클라이언트는 전체 공지(`announcement`)를 초록색, 실제 입장·퇴장(`join`/`leave`)을 노란색으로 채팅 기록에 표시한다. 일반 채팅 발신자로 취급하거나 말풍선을 만들지 않는다.
- 실제 가입이 확정된 뒤 자신을 포함한 현재 가입자에게 입장 알림을 한 번 보낸다. 초기 `DirectoryPage`는 과거 입장 알림을 만들지 않는다. 거절·롤백된 가입이나 가입 전 TCP 종료에도 입퇴장 알림을 만들지 않는다.
- 가입자가 실제로 닫히면 명단에서 제거하고 `PlayerLeft` 뒤에 퇴장 알림을 한 번 보낸다. 중복 종료 콜백은 추가 알림을 만들지 않는다. 닉네임 변경 자체에는 입퇴장 알림이 없으며, 퇴장 문구에는 퇴장 시점의 최종 승인 이름을 사용한다.
- 운영자용 C++ API는 `SummitServerBackend::Announce(std::string_view)`이다. 호출자는 입력 문자열을 소유한 작업을 Host JobRunner에 넣는다. UTF-8 1~512바이트, ASCII 공백만 있는 문자열과 C0/DEL 금지 규칙은 채팅과 같으며, 콘솔 입력도 별도로 UTF-8을 검증한다. 잘못된 입력은 `InvalidArgument`, 자원 실패는 `PlatformError`를 반환한다. 플레이어 채팅의 500ms 제한은 운영자 공지에 적용하지 않는다.
- 클라이언트에서 보내는 `Announce`나 `ServerNotice` handler는 없다. 해당 타입을 보내면 다른 알 수 없는 요청처럼 엄격한 Dispatcher 정책에 따라 연결을 종료한다. 운영자 공지는 네트워크를 통한 관리자 인증 기능을 제공하지 않는다.
- 공지는 현재 가입자만 받으며 가입 대기 연결에는 보내지 않는다. 빈 방에서도 유효한 공지를 받아 로그에 기록하지만, 이후 가입자에게 재생하지 않는다. 성공 반환은 서버 방송 처리가 수락되었다는 뜻이고 실제 수신을 확인하는 ACK는 아니다.
- 알림 전송 중 발생한 종료는 진행 중인 방송이 끝난 뒤 처리한다. 개별 송신 실패 수신자는 종료하고 나머지 수신자에게는 계속 전달한다. 입퇴장 채팅 문구를 만들 메모리가 부족하면 경고를 남기며 이미 확정된 명단을 되돌리지 않는다. 실제 명단 메시지의 일관성 처리는 위 가입·퇴장 규칙을 유지한다.

## 운영자 플레이어 목록

서버 콘솔의 `/players`는 **현재 가입이 승인된 플레이어**를 ID 오름차순으로 출력한다. 가입 전 TCP 연결, 거절·롤백된 가입과 이미 퇴장한 플레이어는 제외한다. 닉네임과 캐릭터는 조회 시점의 최종 승인 값이며, AOI의 가시 범위와 관계없이 전체 명단을 보여 준다.

```text
/players
Players: 2 (joined)
{"character":0,"characterName":"A","id":"1","name":"용사"}
{"character":5,"characterName":"tdw","id":"2","name":"상여자"}
```

총인원 헤더 다음에는 플레이어마다 JSON 한 줄이 나온다. `id`는 정밀도를 보존하는 10진수 문자열이고, `character`는 0~5, `characterName`은 A·B·C·D·E·tdw다. 닉네임은 기존 이벤트 로그와 같은 JSON escape를 거쳐 따옴표나 역슬래시가 있어도 행을 깨뜨리지 않는다. 빈 명단은 `Players: 0 (no joined players)`로 표시한다. `/players`는 인자를 받지 않는다.

콘솔 입력 스레드는 게임 상태를 직접 읽지 않는다. Host JobRunner의 작업이 `SummitServerBackend::SnapshotPlayers()`를 호출하고, Backend의 직렬 Operation 안에서 승인된 프로필을 소유하는 복사본을 만든다. 명단 조회에는 세션 참조나 네트워크 전송이 없으며, 반환 후 발생한 변경은 다음 조회에서 확인한다. 전체 출력 블록을 준비한 뒤 표준 출력에 쓰므로 준비 실패를 빈 명단으로 보고하지 않는다. 조회·출력 준비의 시간과 임시 메모리는 가입 인원에 비례하며, 출력 대상이 느리면 이 운영자 명령도 JobRunner를 기다리게 할 수 있다. 대량 조회를 반복하는 부하 측정 도구로 사용하지 않는다.

이 명령은 서버의 로컬 콘솔 기능이며 클라이언트 프로토콜 요청이 아니다. 명단 조회로 AOI·가입·채팅 상태를 변경하지 않는다.

실행 중인 서버에 새 빌드가 자동 반영되지는 않는다. 새 기능은 사용자가 새 실행 파일로 서버를 시작했을 때 적용한다. 같은 주소/포트의 TCP·UDP 서버를 동시에 실행하지 않으며, 배포 중 기존 프로세스를 임의로 종료하거나 실행 파일을 덮어쓰지 않는다.

## 검증

빌드와 CTest 실행은 [README](README.md#빌드), 공개 소스의 실행 구성과 결과는 [검증 기록](docs/VALIDATION.md)을 확인한다. 아래 내용은 각 검사의 범위이며 특정 환경의 처리량을 나타내지 않는다.

`SummitServerTests`는 A~E의 입장/변경, 두 전용 이름의 tdw 자동 적용과 정확한 공백 비교, 직접 tdw 선택 거절, 신규 접속자의 최종 프로필, 오래된 이동 패킷과 함께 기존 프로필의 원자적 변경, 중복/잘못된 값 거절, 이전 이름 재사용, 입장 전 변경, 전송 실패/예외를 검사한다. `tests/LoopbackIntegration.ps1`은 실제 서버와 세 TCP 클라이언트로 입장, 재시도, 중계, C/D/E 선택, tdw 입장·변경·복귀·직접 선택 거절, 뒤늦게 접속한 클라이언트의 목록과 종료를 검사한다.

다른 스레드의 종료와 Join이 겹치는 경우, 전송 중 동기 종료 콜백이 들어오는 경우, 연결 정원 전체의 종료 알림이 누적되는 경우도 backend 회귀 검사에 포함한다.

서버 알림 검사는 자기 입장 포함, 명단 재전송·가입 거절·롤백의 알림 제외, 최종 승인 이름으로 한 번만 퇴장, 운영자 입력의 UTF-8/512바이트/제어문자 경계와 로그 escape, 가입 대기 연결 제외·이력 미재생, 클라이언트의 공지 위조 요청 거절을 포함한다. 송신 실패·예외와 동기 종료에서도 공지 및 실제 입퇴장 순서를 확인한다.

`SummitServerAoiTests`는 초기 페이지의 단일 ACK 창·송신 시점 최신 프로필·페이지 도중 삭제와 신규 입장, 음수 좌표·진입/이탈 경계·히스테리시스·관찰자 순간이동, 전체 초기 상태·시퀀스 정밀도·최신 상태 병합, 긴 escape 문자열의 배치 상한, 상태 송신 backpressure 회복, tick 중 종료 순서, 잘못되거나 중복된 ACK·동기화 기한·월드 좌표 경계를 검사한다. 수천 명의 분산·밀집 처리량과 CPU·큐·지연은 별도 부하 검증으로 측정해야 한다.

채팅은 backend 및 실제 TCP 검사에서 송신자 포함 중계, 서버 ID/닉네임/시간, 한글·이모지·공백 보존과 정확히 512바이트인 본문, 잘못된 본문 거절 후 연결 유지, 세션별 연속 전송 제한과 회복, 프로필 변경 전후 이름 스냅샷, 뒤늦게 접속한 클라이언트에 과거 대화 미재생을 확인한다. Backend는 모든 C0/DEL 제어문자, 잘못된 본문 타입, 오류 응답의 `seq` 보존, 전송 실패·예외와 퇴장 순서도 검사한다. TCP 검사는 v4 입장 거절, 입장 전 채팅의 최종 거절, 잘못된 UTF-8 프레임의 연결 종료도 확인한다.

## 운영 로그

서버는 시작 전에 콘솔 로거를 연결한다. ServerCore 내부 경고와 오류는 `[WARN]`, `[ERROR]` 등의 수준과 함께 표준 오류(stderr)로 출력하며, 여러 서버 스레드의 로그 한 줄이 서로 섞이지 않도록 직렬화한다.
